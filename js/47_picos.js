document.addEventListener('DOMContentLoaded', () => {
    const container = document.getElementById('view-47_picos');
    if (!container) return;

    // Redirige automáticamente a la web externa
    window.location.href = 'https://47-picos-git-main-jorge-acebes.vercel.app/';
});
